# Remaining Data Gaps & Human Decisions (Deliverable J) + Next Implementation Plan (Deliverable K)

**Update (extension pass, same night):** a second pass verified and analyzed the final rebuild of Munich's six official lists — see `09-final-list-rebuild-analysis.md` through `15-final-calibration-implementation-plan.md`. That pass closed one gap this file originally flagged (item 7 below, Munich's themed-list definitions, was "not read" at the time this file was first written — it has now been fully read, verified against live production, and analyzed in depth) and added new gaps/decisions, folded into the lists below in place rather than only appended, so this file stays a single accurate reflection of open items rather than two overlapping versions.

## J. Remaining data gaps and human decisions

### Data gaps (technical/access limits, disclosed rather than guessed around)

1. **`public.categories` is not readable by the `AGENT_SERVICE_DATABASE_URL` role.** Category names throughout this analysis were inferred from sample item bodies grouped by `category_id`, not read directly. High confidence for the 11 categories matching `defaultMetroManifest.ts`'s own names; the 12th ("Play") and a small (2-item) unidentified bucket are inferred, not confirmed.
2. **This role appears to sit behind RLS that hides `is_active=false` rows entirely.** A direct `SELECT` for the 21 retired original UUIDs returned 0 rows (vs. all 9 rewrite UUIDs returning cleanly), and a metro-wide inactive-item count returned 0. This means the 21 retirements' current state rests on `munich_original70_cleanup.sql`'s own successful `COMMIT` and postflight assertion (`RAISE EXCEPTION` on any count mismatch, which did not fire), not a fresh live read. **A privileged connection would be needed to independently re-confirm this.**
3. **The 40 (of 70) original items not touched by the cleanup SQL were name-matched, not body-diffed, against production.** ~47 were confirmed present under a matching venue name (some discrepancy from 40 vs. ~47 reflects imprecise venue-name extraction from the source SQL's body text, documented in `01-original-70-disposition.md`). Venue survival was confirmed; verbatim body survival was not, for any of them.
4. **7 of the 21 retired original UUIDs could not be matched to a specific pre-cleanup body/venue name** even via the elimination method used for the other 14, due to punctuation/quote-character mismatches in the extraction regex against the source SQL. Their retirement reasons are therefore entirely unknown.
5. **No artifact records what the Bulk Add Agent rejected, held, or left unprocessed.** All four CSVs are accepted-candidate lists only. Part 4's classification of "recurring rejection reasons" is built entirely from indirect evidence (what got in vs. what a reasonable researcher should have found and isn't there), not from any actual decision log.
6. **Wave1/wave3/final-selective-fill have no equivalent SQL insertion artifact** the way wave2 does. Their CSV "accepted" status could not be independently confirmed as "actually inserted" the way wave2's 39-CSV-rows-to-39-INSERTs match could.
7. ~~Munich's Fall 30 / themed-list definitions were not read~~ **RESOLVED by the extension pass.** All six official lists' final membership (and, via `munich_home_list_patch_2026-09-14_DRIVER_CERTIFIED_NOT_APPLIED.sql`, Winston's original four-list baseline) have now been read, verified live, and analyzed in full — see `09-final-list-rebuild-analysis.md` through `12-m9-training-requirements.md`. Failure 10 in `05-pipeline-failure-stage-mapping.md` ("weak themed-list/Fall 30 membership," previously marked unresolved) is now evaluable: the original Fall 30/After Dark/Cafés/Hidden Gems lists were confirmed weak (generic actions, near-zero discovery mechanics, no nighttime discipline), and the final rebuild is a near-total replacement (only 2 of 62 original list-membership rows survived by venue identity — `10-original-vs-final-list-decisions.md`) rather than an incremental fix.
8. **Catalog voice/tone consistency (M8.75's domain) was not separately evaluated** — reading all 184 bodies for voice consistency was out of scope given the volume; only generic-action and malformed-name heuristics were run.
9. **Combined-business rate on the *live* catalog was not exhaustively measured.** A SQL heuristic (4+ apostrophes in a body) was tried and produced only false positives (legitimate quoted phrases). Finding more Beirut-Beirut-style combined items in the live 184 would require reading all bodies manually, which was not done.
10a. **(New, extension pass)** `public.categories` remains unreadable to this role — confirmed again during the extension pass via a direct retry (`permission denied for table categories`). Category names used throughout the extension's list scorecards (`11-list-portfolio-scorecards.md`) are still content-inferred from sample bodies per `category_id`, now cross-checked a second time against a list-scoped sample rather than the original session's broader-catalog sample, but still not a direct read.
10b. **(New, extension pass)** Whether `public.list_items` carries any audit-trail-style column (e.g. an `updated_at`) that a "successful no-op" rerun of the list-rebuild SQL would disturb was not checked — `13-list-sql-generation-safeguards.md`'s idempotency analysis flags this as a minor, unconfirmed gap rather than asserting either way.
10c. **(New, extension pass)** Whether the two branches in the SQL's After Dark rename-vs-delete logic (`$setup$` block) actually fired the rename path or found a no-op state cannot be distinguished after the fact from list state alone — both possible paths converge on the same live result the SQL's own assertions require, so this is disclosed as an inert ambiguity, not a real gap (see `09-final-list-rebuild-analysis.md`).
11. **Live secret-evidence and ownership-verification counts are trivially zero across the board** (confirmed directly via SQL), which is itself strong evidence rather than a gap — but *why* they're zero (a genuine absence of secrets in Munich vs. a tooling gap that never captured them) can only be inferred from the `research_verifier` code-comment evidence, not proven from Munich's data alone.

### Human decisions still needed (not something more research can resolve)

0. **(Elevated by the extension pass — the two most launch-visible open items in the whole analysis.)**
   - **Difficulty and secret status.** All 184 active Munich items are still `difficulty = 1` and `is_secret = false`, with no exceptions — unchanged by the list rebuild and unchanged since the original session. `14-difficulty-and-secret-remediation-plan.md` (extension pass) provides a full evidence-based rubric plus a worked, real-item review queue (Café Frischhut, Therme Erding, Maisinger Schlucht for difficulty; Haus im Tal, 'Boazn' at Ludwigsbrücke, Alva Morgaine for secret status) — but this is a plan only, with zero executable remediation SQL. This is now the single most "ready to act on" open item in the whole Munich analysis: the rubric and queue exist, only Jerry's review and a separate, explicit remediation pass are missing.
   - **Dachau and Starnberg.** Already flagged below as item 1 — the extension pass adds that both are now curated into official, featured-eligible lists (Day Trips & Big Adventures, and Starnberg also in Cafés/Markets & Local Flavor), not just present in the catalog. See the updated item 1 for detail.
1. **Reconcile `m0.json`'s `geographicScope` with the live 36-neighborhood catalog — and now also with the live, featured-eligible list state.** Someone (Jerry) needs to decide and record: is the surrounding-municipality tier (Dachau, Starnberg, Erding, Freising, etc.) now permanently in scope for Munich, or should it be treated as a separate "Munich Day Trips" sub-product? Either answer is fine; the current silent contradiction is not. **Extension-pass update:** neither `munich_lists_rebuild_no_temp_tables.sql` nor the wave CSVs record an explicit decision on this — Dachau and Starnberg's presence in official Day Trips/Cafés-Markets list membership is new evidence of *de facto* inclusion (real, promoted, featured-eligible list content), not a substitute for Jerry's explicit confirmation. **Still open.**
2. **Decide the Kunst Oase / Vereinsheim precedent.** Are same-venue-different-wording pairs allowed to coexist as genuinely distinct experiences (Kunst Oase's case is defensible — different physical zones), or should the Vereinsheim pair (same recurring event, described twice) be collapsed to one item? This is an editorial-policy decision, not something `seedDuplicateNormalization.ts`'s clustering alone can resolve — the module deliberately never auto-decides.
3. **Decide whether the 16 single-item neighborhoods need backfill before Munich is considered "launch-complete" at its current geographic breadth**, or whether thin-but-present coverage is an acceptable tradeoff for breadth.
4. **Prioritize the `research_verifier` evidence-contract work.** This is referenced as a "known follow-up, not fixed by this stage" in `seedPortfolioAudit.ts`'s own code — this analysis independently arrives at the same stage being the highest-leverage remaining gap from the Munich data (it is the root cause behind both the ownership/commercial-mix gap and the secret-mechanic gap). This is a scoping/prioritization decision for Jerry, not something this analysis can decide on its own.
5. **Decide whether to grant a broader DB role for future forensic/audit work**, given that `public.categories` and inactive rows were unreadable here — future audits of this kind will hit the same wall unless a read-only-but-broader role is set up.

## K. Exact next implementation plan (isolated commits, NOT executed as part of this task)

This task's constraints are explicit: no code changes except the standalone fixture already created, no final list SQL, no Munich reorganization. The following is a proposed sequence for Jerry to review and greenlight separately, each commit independently revertable:

1. **Commit: `research_verifier` evidence-contract MVP.** Extend whatever produces `RawCandidate`s today to optionally populate `ownershipType`, `isSecretClaimed`, and `secretEvidence` (the exact fields `seedPortfolioAudit.ts` already duck-types for). No consumer behavior changes required in this commit — it only makes the fields real. This unblocks the two highest-leverage gaps found in this analysis (ownership verification, secret-mechanic verification) and should land first.
2. **Commit: `munichGoldStandard.test.ts`.** Wire the already-created `agent-service/playbooks/__fixtures__/munichGoldStandard.ts` into real assertions against `evaluateSeedCandidate` + `detectSeedDuplicateClusters`, per the recommendation in `06-gold-standard-fixture-proposal.md`. Small, isolated, no production-code changes — pure test addition.
3. **Commit: name-hygiene check for candidate names.** A small pure function (e.g. `checkVenueNameHygiene` alongside `editorialDistinctiveness.ts`'s existing pattern) flagging run-on/sentence-shaped names, wired into `seedPortfolioAudit.ts`'s `evaluateSeedCandidate` as an additional REJECT/HOLD reason. Includes its own unit tests using the real Flaucher/Hirschgarten/Dallmayr examples from `01-original-70-disposition.md`.
4. **Commit: combined-business check.** A small pure function flagging candidates whose name/claim text contains two distinct proper-noun businesses joined by "and"/"or," wired the same way, with its own tests using the real Beirut Beirut/Backsteinchen example.
5. **Commit: "Play" category normalization.** Either add "Play" formally to `DEFAULT_CATEGORY_COVERAGE_PLAN` with its own minimum/target, or add an explicit normalization map folding Play-shaped raw category strings into Adventure/Sports before `evaluateCategoryPolicies` runs — whichever Jerry prefers architecturally. Isolated, category-policy-only change with its own tests.
6. **Commit: m0.json-vs-live-geography reconciliation check at M10.** A pure function comparing a metro's `geographicScope` record against its live/proposed neighborhood set, flagging any live neighborhood not accounted for in the scope record. Wired as a (non-blocking, initially) M10 warning so it can be observed against Munich's real, already-contradictory state before deciding whether to make it a hard gate.
7. **Commit (policy, not code): update `docs/metro-launch-audit/munich/metro-area-facts.json`/`m0.json`'s `geographicScope` text** to reflect the actual, currently-live 36-neighborhood boundary — this is the cheapest possible fix for the most concrete contradiction found in this analysis, and could reasonably be done independently of and before any of the code commits above, pending Jerry's decision item 1 in the human-decisions list.

Each commit is scoped to be revertable independently and does not depend on the others except #2 depending on #1 only for the secret-evidence-with-real-data test case (which can otherwise use the fixture's synthetic evidence record as-is).

### K-extension. Additional proposed commits from the extension pass (also NOT executed)

See `15-final-calibration-implementation-plan.md`'s synthesis for the full prioritized ordering across both passes. New items specific to the extension:

8. **Commit (policy, not code): reconcile `m0.json`'s `geographicScope` against BOTH the live 36-neighborhood catalog AND the live, featured-eligible list membership** — supersedes item 7 above in urgency once Jerry confirms the Dachau/Starnberg decision (item 1 in the human-decisions list above), since the extension pass found both municipalities are now promoted in official lists, not just present in the catalog.
9. **Commit: `munichListCurationFixtures.test.ts`.** Wire the already-created `agent-service/playbooks/__fixtures__/munichListCurationFixtures.ts` into real assertions once an `ItemListFitDecision`-shaped M9 implementation exists (proposed design only in `12-m9-training-requirements.md`, not yet built) — mirrors item 2 above for the list-curation dimension.
10. **Commit: UUID-based list-SQL generation standard.** Document (and, once agreed, enforce via a lint/template) that any future list-membership SQL resolve items to production UUIDs once via a preflight body-match pass, then write the actual `INSERT`/patch SQL keyed only by UUID — never re-resolve by body text as the operational mechanism on every run. See `13-list-sql-generation-safeguards.md` for the full analysis and the concrete Schumann's Bar/Pusser's apostrophe incident this would have prevented entirely (a UUID literal has no escaping hazard).
11. **Commit (policy, not code): schedule the difficulty/secret remediation pass** proposed in `14-difficulty-and-secret-remediation-plan.md` — a rubric and a real item-level review queue already exist; only Jerry's review of the rubric's band boundaries and a separate, explicit UUID-keyed remediation SQL (not written as part of either analysis pass) remain.
