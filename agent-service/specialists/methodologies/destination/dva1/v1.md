# DVA-1 Methodology — v1 (GATE SEMANTICS ONLY — full methodology not yet recovered)

`methodologyId: destination/dva1` `version: v1` `owningSpecialist: destination_strategist`

## What this file is, and is not

This is **not** the DVA-1 Claude Project's actual question set, scoring rubric, or analytical
methodology. Per the explicit Phase 2D instruction not to reconstruct missing instructions from
Open Brain summaries, and not to invent what can't be recovered: **the full DVA-1 methodology
could not be recovered from this repository or from Open Brain.** It exists today only inside a
dedicated Claude Project Jerry runs directly.

## What IS recovered (Open Brain, 2026-07-12, Jerry's own words — retrieved verbatim)

> DVA-1 is a fast screener... 5-10 minutes... score thresholds: 90-100 Elite, 80-89 Excellent,
> 65-79 Borderline/Jerry decision, below 65 Archive... DVA-2 never starts automatically; Jerry
> must explicitly approve moving forward in every case.

This gives Chief exactly what it needs to **route** a DVA-1 result, never to **produce** one:

| Score range | Tier | `classifyDVA1Score()` |
|---|---|---|
| 90–100 | ELITE | proposes DVA-2 |
| 80–89 | EXCELLENT | proposes DVA-2 |
| 65–79 | BORDERLINE | proposes DVA-2 (Jerry decision) |
| < 65 | ARCHIVE | does not propose DVA-2 |

`dva1RequiresJerryApproval()` always returns `true` — zero exceptions, including a 100-score
ELITE result. This is implemented exactly in `agent-service/playbooks/destinationHubLifecycle.ts`.

## What Chief does with a DVA-1 execution today (`EXECUTOR_UNAVAILABLE`)

There is no programmatic way to invoke the DVA-1 Claude Project (see
`agent-service/playbooks/destinationExecutorGap.ts`). A `SpecialistExecutionRequest` against this
methodology run through `REMOTE_AI_EXECUTOR` or `LOCAL_TOOL_EXECUTOR` must return
`EXECUTOR_UNAVAILABLE` honestly, not a fabricated result. The only working path today is
`MANUAL_EXECUTOR`: Chief builds a structured assignment package (destination name/context/
objective), a human (Jerry, or a Claude session Jerry directs) runs it in the actual DVA-1
Project, and hands the resulting artifact back as a `DVA1Artifact` for Chief to validate and gate
on.

## Exact one-time input still needed from Jerry to close this gap

Export the DVA-1 Claude Project's full system/project instructions (the actual question set,
scoring rubric, and analytical guidance — not just the gate thresholds above) as a single document
(paste, `.md` export, or a share link Chief can fetch once). Once received, replace this file's
content with the real methodology and bump to `v2`, updating `methodologyId`/`version` references
in any execution that used `v1`'s gate-only contract. Do not paraphrase or reconstruct the
methodology from memory in the meantime — the gate semantics above are sufficient for Chief to
route results Jerry hands it; they are not sufficient to run DVA-1 itself.
