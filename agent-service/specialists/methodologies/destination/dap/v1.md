# DAP Methodology — v1 (GATE SEMANTICS ONLY — SUPERSEDED by v2)

> **SUPERSEDED (Phase 2G, 2026-09-04):** Jerry has since provided the real,
> verbatim DAP (v2.0) Claude Project instructions. See `v2.md` (the actual
> methodology, `complete: true` in the registry) and
> `v2.legacy-operator-instructions.md` — including the new `rightNowTask`
> extracted field this file's placeholder didn't know about. This file is
> kept for historical comparison only; do not execute against it —
> `methodologyRegistry.ts` marks it `complete: false`.

`methodologyId: destination/dap` `version: v1` `owningSpecialist: destination_strategist`

## What this file is, and is not

Same status as DVA-1/DVA-2: the actual DAP ("Destination Action Plan") Claude Project's real
question set and analytical methodology could not be recovered from this repo or Open Brain. This
file records only the retrieved entry condition and the structured extraction contract Chief reads
off a completed DAP artifact — never a competing methodology.

## What IS recovered (Open Brain, 2026-07-12)

> DAP takes a completed DVA-2 as its input.

`dapEntryConditionMet()` in `destinationHubLifecycle.ts`: DAP only starts once DVA-2's
recommendation is `GREEN`. `validateDapInput()` hard-rejects a DAP artifact whose
`consumedDva2ArtifactRef`/`destinationId` doesn't match the DVA-2 artifact on file for that
destination — same isolation discipline as DVA-1→DVA-2.

## Structured fields Chief extracts (never re-derives) from a completed DAP artifact

`recommendedChampion`, `secondaryChampions`, `decisionMakers`, `stakeholderOrganizations`,
`fundingBudgetClues`, `likelyBuyer`, `estimatedSalesDifficulty`, `timingConsiderations`,
`politicalStakeholderComplexity`, `objectionsHurdles`, `destinationPainPoints`,
`checkoffValueProposition`, `recommendedEntryStrategy`, `relationshipSequence`,
`recommendedOfferDirection`. These are operational projections for Chief — the full DAP artifact
text remains the source of truth; the extracted fields are what `destination_commercial/v1.md`'s
6A ("Champion/Stakeholder Selection") and downstream stages consume.

## Executor status

Same as DVA-1/DVA-2: `EXECUTOR_UNAVAILABLE` for any automated executor type; `MANUAL_EXECUTOR` is
the only working path today.

## Exact one-time input still needed from Jerry

Export the DAP Claude Project's full instructions (how it derives champion/stakeholder/commercial
intelligence from a DVA-2 artifact). Replace this file's content and bump to `v2` once received.
